# Automatic HTTPS via Let's Encrypt. {$DOMAIN} comes from the .env file.
{$DOMAIN} {
	encode gzip zstd
	reverse_proxy web:3000
}

# www → apex, with its own certificate.
www.{$DOMAIN} {
	redir https://{$DOMAIN}{uri} permanent
}
